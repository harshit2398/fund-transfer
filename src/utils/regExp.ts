

// Only for number with decimal
export const numberDecimalRegExp = RegExp(/^\d+(\.\d{0,2})?$/);

//  Bank account regex
export const accountNoRegExp=RegExp('^[0-9]{9,18}$');

//regex=”^[0-9]{9,18}$”   OR regex=”^\d{9,18}$”
